{
	"name": "mongodbext",
	"description": "Quick and dirty (monkey patching) extensions for mongodb-native",
	"version": "0.1.0",
	"repository": {
		"type": "git",
		"url": "git://github.com/2do2go/mongodbext.git"
	},
	"keywords": [
		"mongodb", "db", "hook", "trigger"
	],
	"scripts": {
		"test": "mocha test --reporter spec --bail --async-only"
	},
	"main": {
		"east": "./lib/mongodbext"
	},
	"dependencies": {
		"mhook": "0.1.1",
		"mongodb": "1.3.6"
	},
	"devDependencies": {
		"mocha": "1.9.0",
		"expect.js": "0.2.0"
	}
}
